"""
ai_writer.py — переписывает новость в голос Велес IT.

Если ANTHROPIC_API_KEY задан — использует Claude (живой текст).
Если пусто — шаблонная генерация (всегда работает).
"""
import json
import random
from config import CTA_VARIANTS, USE_AI, ANTHROPIC_API_KEY


# === Шаблонные блоки в тоне Велес IT ===

CATEGORY_HOOKS = {
    "ransomware": [
        "Шифровальщик вошёл — бизнес встал на неделю",
        "Одно письмо — и вся компания в зашифрованных файлах",
    ],
    "leak": [
        "Миллионы записей — в открытом доступе",
        "База клиентов утекла. Никто ничего не взламывал.",
    ],
    "phishing": [
        "Сотрудник кликнул — компания вскрыта за 20 минут",
        "Поддельное письмо от поставщика — реальная потеря миллионов",
    ],
    "ddos": [
        "Сайт лёг на 7 часов в чёрную пятницу",
        "DDoS-вымогательство: плати или стой без продаж",
    ],
    "ecommerce": [
        "Скиммер на чекауте: магазин крадёт карты — и не знает об этом",
        "Magecart взял ещё один интернет-магазин",
    ],
    "vulnerability": [
        "Дыра, через которую заходят все",
        "Уязвимость в плагине — взлом всего сайта",
    ],
    "api": [
        "API открыт — данные тоже",
        "Интеграция, которая сливает базу клиентов",
    ],
    "general": [
        "Ещё один способ, как взламывают такие как вы",
    ],
}

CATEGORY_LABELS = {
    "ransomware": "АТАКА", "leak": "УТЕЧКА", "phishing": "АТАКА",
    "ddos": "АТАКА", "ecommerce": "АТАКА", "vulnerability": "УГРОЗА",
    "api": "УГРОЗА", "general": "НОВОСТЬ",
}

CATEGORY_EMOJI = {
    "ransomware": "🔒", "leak": "💧", "phishing": "🎣", "ddos": "💥",
    "ecommerce": "🛒", "vulnerability": "🕳", "api": "🔌", "general": "⚠️",
}

CONSEQUENCES_BY_CATEGORY = {
    "leak": (
        "— клиенты получают волну фишинговых писем «от вашего магазина»\n"
        "— штраф по 152-ФЗ — до 18 млн рублей за повторное нарушение\n"
        "— отток покупателей: 64% не возвращаются после публичной утечки\n"
        "— репутационный урон: новость живёт в поиске годами"
    ),
    "ransomware": (
        "— простой бизнеса от 3 дней до 2-3 недель\n"
        "— требование выкупа в среднем $250К для среднего бизнеса\n"
        "— потеря данных, если бэкапы не отделены от основной сети\n"
        "— штрафы и иски от клиентов"
    ),
    "ecommerce": (
        "— возврат денег пострадавшим клиентам ($150-200 за карту)\n"
        "— блокировка эквайером, потеря возможности принимать платежи\n"
        "— проверка от платёжной системы и штрафы PCI DSS\n"
        "— иски от клиентов и публичные разборы в СМИ"
    ),
    "ddos": (
        "— простой сайта в самый прибыльный момент\n"
        "— потеря клиентов, которые ушли к конкурентам и не вернулись\n"
        "— стоимость экстренного подключения защиты ×3-5 от обычного\n"
        "— риск повторных атак — атакующие маркируют слабые цели"
    ),
    "phishing": (
        "— компрометация бухгалтерии, утечка финансовых документов\n"
        "— несанкционированные платежи и переводы поставщикам\n"
        "— компрометация админки сайта через украденные пароли\n"
        "— атака распространяется на контрагентов из переписки"
    ),
    "general": (
        "— репутационный риск\n"
        "— возможные финансовые потери\n"
        "— риск компрометации клиентов\n"
        "— проверки регуляторов"
    ),
}

SOLUTION_BLOCKS = [
    (
        "Большинство таких инцидентов — не «хакерские атаки», а открытые "
        "админки, забытые тестовые серверы и кривые настройки облаков. "
        "Это всё видно снаружи и проверяется.\n\n"
        "Велес IT за 15 минут смотрит ваш сайт глазами атакующего и "
        "показывает 1-2 такие зоны риска. Бесплатно, без презентаций, "
        "без продажи."
    ),
    (
        "Подобные сценарии повторяются десятками за год — меняются только "
        "названия жертв. Большинство дыр давно известны. Дело только в том, "
        "чтобы найти их у себя раньше них.\n\n"
        "Велес IT делает экспресс-аудит сайта бесплатно. 15 минут, снаружи, "
        "без доступа в инфраструктуру."
    ),
]


def get_category_label(category: str) -> str:
    return CATEGORY_LABELS.get(category, "НОВОСТЬ")


def cta() -> str:
    return random.choice(CTA_VARIANTS)


# ============================================================
# CLAUDE API: живая генерация (включается, если задан ANTHROPIC_API_KEY)
# ============================================================

VELES_VOICE_PROMPT = """Ты — редактор Telegram-канала Велес IT (компания по кибербезопасности, пентест и защита бизнеса).

Перепиши новость в фирменном тоне. Длина итогового поста — 1200-1500 символов.

ТОН:
- Без воды, без терминов, язык предпринимателя
- Через риск (деньги, клиенты, простой) — не через техничку
- Прямо, жёстко, иронично
- ЗАПРЕЩЕНО: «комплексные решения», «киберщит», «хакер в капюшоне», «надёжная защита»

ПРОПОРЦИИ:
70% — фактура, контекст, цифры
30% — последствия для бизнеса + мягкая подводка к Велес IT (только В ФИНАЛЕ)

Верни СТРОГО JSON со всеми полями, без markdown-обёртки:
{
  "hook": "заголовок 5-8 слов с цифрой/глаголом",
  "lead": "2-3 предложения о факте с цифрами",
  "details": "4-6 предложений: как именно вошли, кто, детали инцидента",
  "context": "2-3 предложения: тренд, кого ещё били по такому же сценарию",
  "consequences": "4 пункта последствий ДЛЯ ВЛАДЕЛЬЦА БИЗНЕСА, каждый с тире в начале и переводом строки",
  "solution": "мягкая подводка: какие компании в зоне риска и как Велес IT помогает (3-4 предложения, без агрессивной продажи)",
  "fact_label": "короткий лейбл для фактоида на картинке (3-5 слов в нижнем регистре)",
  "fact_value": "цифра или короткая строка для большого выделения"
}"""


def rewrite_with_claude(news_item: dict, category: str) -> dict:
    """Живая переписка через Claude API."""
    import anthropic
    client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)

    user_text = (
        f"Категория: {category}\n"
        f"Заголовок оригинала: {news_item['title']}\n"
        f"Текст: {news_item['summary']}"
    )

    msg = client.messages.create(
        # Haiku 4.5 ($1/$5 за 1M токенов) — отлично для перефраза.
        # Для премиум-качества меняй на "claude-opus-4-7" ($5/$25).
        model="claude-haiku-4-5-20251001",
        max_tokens=2000,
        system=VELES_VOICE_PROMPT,
        messages=[{"role": "user", "content": user_text}]
    )
    text = msg.content[0].text.strip()

    # Сносим возможную markdown-обёртку ```json ... ```
    if text.startswith("```"):
        text = text.split("```")[1]
        if text.startswith("json"):
            text = text[4:]
        text = text.strip()

    return json.loads(text)


# ============================================================
# Главная точка входа
# ============================================================

def rewrite_in_veles_tone(news_item: dict) -> dict:
    """
    Возвращает структурированный пост-словарь.
    Если есть Claude API — использует его. Иначе — шаблоны.
    """
    category = news_item.get("category", "general")

    # Дефолты, которые работают всегда
    default = {
        "hook": random.choice(CATEGORY_HOOKS.get(category, CATEGORY_HOOKS["general"])),
        "emoji": CATEGORY_EMOJI.get(category, "⚠️"),
        "category": category,
        "lead": news_item["summary"][:200],
        "lead_short": news_item["summary"][:120],
        "details": news_item["summary"][200:600] if len(news_item["summary"]) > 200 else (
            "Детали инцидента уточняются. По первым данным, атакующие действовали "
            "по типичной для этой категории схеме — без сложных инструментов, "
            "через известные точки входа."
        ),
        "context": (
            "Это часть тренда: подобные инциденты в этой категории стали "
            "в 2025–2026 происходить кратно чаще. По разным оценкам индустрии, "
            "удар получает каждый шестой средний бизнес в e-commerce за год."
        ),
        "consequences": CONSEQUENCES_BY_CATEGORY.get(category, CONSEQUENCES_BY_CATEGORY["general"]),
        "solution": random.choice(SOLUTION_BLOCKS),
        "source_link": news_item["link"],
        "fact_label": "минут на бесплатный аудит",
        "fact_value": "15",
        "attack_step_1": "нашли публично доступную уязвимость в админке",
        "attack_step_2": "подобрали слабый пароль из утёкших баз",
        "attack_step_3": "закрепились через забытую тестовую учётку",
    }

    if not USE_AI:
        return default

    # Пробуем через Claude. Если падает — откатываемся на шаблоны.
    try:
        ai_result = rewrite_with_claude(news_item, category)
        default.update({
            "hook": ai_result.get("hook", default["hook"]),
            "lead": ai_result.get("lead", default["lead"]),
            "details": ai_result.get("details", default["details"]),
            "context": ai_result.get("context", default["context"]),
            "consequences": ai_result.get("consequences", default["consequences"]),
            "solution": ai_result.get("solution", default["solution"]),
            "fact_label": ai_result.get("fact_label", default["fact_label"]),
            "fact_value": ai_result.get("fact_value", default["fact_value"]),
        })
        default["lead_short"] = default["lead"][:120]
        return default
    except Exception as e:
        print(f"[ai_writer] Claude error, using templates: {e}")
        return default

import os
from dotenv import load_dotenv

load_dotenv()

# === Telegram ===
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")
TELEGRAM_CHANNEL_ID = os.getenv("TELEGRAM_CHANNEL_ID", "@cyberveles")
TELEGRAM_ADMIN_ID = os.getenv("TELEGRAM_ADMIN_ID")

# Режим: "auto" — постит сразу в канал, "draft" — на одобрение в личку
MODERATION_MODE = os.getenv("MODERATION_MODE", "draft")

# === Бренд ===
SITE_URL = os.getenv("SITE_URL", "https://veles-it.ru")
CHANNEL_HANDLE = "@cyberveles"
CONTACT_HANDLE = os.getenv("CONTACT_HANDLE", "@velesit_audit")

# === AI ===
ANTHROPIC_API_KEY = os.getenv("ANTHROPIC_API_KEY", "")
USE_AI = bool(ANTHROPIC_API_KEY.strip())

# === Файлы ===
SENT_FILE = "sent.json"
PENDING_FILE = "pending.json"
IMAGES_DIR = "images"

# === RSS источники ===
RSS_FEEDS = [
    "https://feeds.feedburner.com/TheHackersNews",
    "https://www.bleepingcomputer.com/feed/",
    "https://www.darkreading.com/rss.xml",
    "https://www.securityweek.com/feed/",
    "https://krebsonsecurity.com/feed/",
    "https://securelist.com/feed/",
]

# === Фильтр тем ===
KEYWORDS_INCLUDE = [
    "data breach", "leak", "leaked", "exposed",
    "ransomware", "ransom", "encrypted",
    "phishing", "spear phishing", "scam",
    "ecommerce", "e-commerce", "magento", "shopify", "woocommerce",
    "vulnerability", "exploit", "zero-day", "0day", "rce",
    "api", "payment", "checkout", "stripe",
    "skimmer", "magecart", "card data",
    "customer data", "pii", "personal data",
    "ddos", "credential", "stealer",
    "sql injection", "xss", "supply chain",
]

KEYWORDS_EXCLUDE = [
    "apt28", "apt29", "lazarus group analysis",
    "kremlin", "geopolit", "election interference",
]

# === CTA ===
CTA_VARIANTS = [
    "Покажем за 15 минут, где у вашего сайта дыры. Без презентаций, без продажи. Пиши в",
    "Если у вас интернет-магазин — атакуем его так, как сделал бы реальный злоумышленник. Первый осмотр бесплатно. Пиши в",
    "Велес IT работает в тени, чтобы ваш бизнес оставался на свету. Бесплатный аудит — пиши в",
    "Не ждите письма с требованием выкупа. Найдём дыры раньше. Пиши в",
]

from config import KEYWORDS_INCLUDE, KEYWORDS_EXCLUDE


def is_relevant(item) -> bool:
    blob = f"{item['title']} {item['summary']}".lower()
    if any(bad in blob for bad in KEYWORDS_EXCLUDE):
        return False
    return any(kw in blob for kw in KEYWORDS_INCLUDE)


def detect_category(item) -> str:
    blob = f"{item['title']} {item['summary']}".lower()
    if any(w in blob for w in ["ransomware", "ransom", "encrypted"]):
        return "ransomware"
    if any(w in blob for w in ["data breach", "leak", "leaked", "exposed", "pii"]):
        return "leak"
    if any(w in blob for w in ["phishing", "spear", "scam"]):
        return "phishing"
    if any(w in blob for w in ["ddos"]):
        return "ddos"
    if any(w in blob for w in ["magecart", "skimmer", "card data", "checkout"]):
        return "ecommerce"
    if any(w in blob for w in ["zero-day", "0day", "rce", "exploit"]):
        return "vulnerability"
    if any(w in blob for w in ["api", "supply chain"]):
        return "api"
    return "general"


def filter_and_diversify(items, n=3):
    relevant = [i for i in items if is_relevant(i)]
    for it in relevant:
        it["category"] = detect_category(it)

    seen = set()
    picked = []
    for it in relevant:
        if it["category"] in seen:
            continue
        picked.append(it)
        seen.add(it["category"])
        if len(picked) == n:
            return picked

    for it in relevant:
        if it not in picked:
            picked.append(it)
            if len(picked) == n:
                break
    return picked

"""
generator.py — посты 1200-1500 символов в формате @cyberveles.
70% контента, 30% — Велес IT в финале.
"""
from ai_writer import rewrite_in_veles_tone, cta, get_category_label
from config import CHANNEL_HANDLE, CONTACT_HANDLE


def build_news_post(news_item: dict) -> dict:
    rw = rewrite_in_veles_tone(news_item)
    text = (
        f"{rw['emoji']} <b>{rw['hook']}</b>\n\n"
        f"{rw['lead']}\n\n"
        f"{rw['details']}\n\n"
        f"{rw['context']}\n\n"
        f"📉 <b>Чем это грозит вам как владельцу бизнеса:</b>\n"
        f"{rw['consequences']}\n\n"
        f"———\n\n"
        f"🎯 <b>Что с этим делать</b>\n\n"
        f"{rw['solution']} <a href=\"{rw['source_link']}\">Источник</a>.\n\n"
        f"{cta()} {CONTACT_HANDLE}\n\n"
        f"{CHANNEL_HANDLE}"
    )
    return {
        "text": text,
        "img_category": get_category_label(rw["category"]),
        "img_headline": rw["hook"],
        "img_dek": rw["lead_short"],
        "img_filename": f"news_{rw['category']}.png",
        "img_fact_label": rw["fact_label"],
        "img_fact_value": rw["fact_value"],
        "kind": "news",
    }


def build_breakdown_post(news_item: dict) -> dict:
    rw = rewrite_in_veles_tone(news_item)
    text = (
        f"🔍 <b>Разбор взлома: {rw['hook']}</b>\n\n"
        f"{rw['lead']}\n\n"
        f"<b>Как именно вошли:</b>\n\n"
        f"🟢 {rw['attack_step_1']}\n"
        f"🟢 {rw['attack_step_2']}\n"
        f"🟢 {rw['attack_step_3']}\n\n"
        f"{rw['context']}\n\n"
        f"📉 <b>Что было бы, если бы это случилось у вас:</b>\n"
        f"{rw['consequences']}\n\n"
        f"———\n\n"
        f"🎯 <b>Как это ловит пентест</b>\n\n"
        f"{rw['solution']}\n\n"
        f"{cta()} {CONTACT_HANDLE}\n\n"
        f"{CHANNEL_HANDLE}"
    )
    return {
        "text": text,
        "img_category": "Разбор",
        "img_headline": rw["hook"],
        "img_dek": "Анатомия атаки. Шаг за шагом.",
        "img_filename": f"breakdown_{rw['category']}.png",
        "img_fact_label": rw["fact_label"],
        "img_fact_value": rw["fact_value"],
        "kind": "breakdown",
    }


def build_lifehack_post() -> dict:
    text = (
        f"⏱ <b>5 минут — и вы знаете о своей безопасности больше, чем 90% бизнесов</b>\n\n"
        f"Большинство владельцев интернет-магазинов узнают о взломе от банка, "
        f"клиентов или СМИ. Не от своей IT-команды. Чтобы это поменять, "
        f"не нужен безопасник в штате — нужно 5 минут и три проверки.\n\n"
        f"<b>Прямо сейчас:</b>\n\n"
        f"🟢 Откройте <b>haveibeenpwned.com</b> и проверьте корпоративную почту. "
        f"Если она светится в утечках — пароли сотрудников уже в базах даркнета. "
        f"Любой может попробовать их на вашей админке.\n\n"
        f"🟢 Зайдите в админку сайта и откройте список пользователей. Найдите "
        f"учётки, которых не помните: «test», «admin2», старые аккаунты разработчиков. "
        f"Это самые частые точки входа.\n\n"
        f"🟢 Проверьте двухфакторку у трёх ключевых людей: владельца, главного "
        f"разработчика, маркетолога с доступом к рассылкам. Если хоть у одного "
        f"нет — это и есть ваша главная уязвимость.\n\n"
        f"Если по любому из пунктов нашлась проблема — у вас уже есть, с чем работать. "
        f"Если всё чисто — вы в топ-10% рунет-бизнесов по гигиене.\n\n"
        f"———\n\n"
        f"🎯 <b>Дальше — глубже</b>\n\n"
        f"Это поверхностный осмотр. Реальные дыры обычно лежат на уровне кода, "
        f"конфигов и сторонних интеграций — туда без пентеста не заглянешь.\n\n"
        f"{cta()} {CONTACT_HANDLE}\n\n"
        f"{CHANNEL_HANDLE}"
    )
    return {
        "text": text,
        "img_category": "Лайфхак",
        "img_headline": "5 минут — и вы впереди 90% бизнесов",
        "img_dek": "Три проверки, которые покажут, кто и как может зайти в ваш сайт",
        "img_filename": "lifehack.png",
        "img_fact_label": "минут на проверку",
        "img_fact_value": "5",
        "kind": "lifehack",
    }


def build_welcome_post() -> dict:
    """Закреп для канала. Запускается один раз вручную: python main.py welcome"""
    text = (
        f"🛡 <b>Это канал Велес IT. Мы взламываем компании раньше, "
        f"чем это делают другие.</b>\n\n"
        f"Велес IT — команда пентестеров. Мы имитируем настоящие хакерские "
        f"атаки на сайты, инфраструктуру и сотрудников, чтобы наши клиенты "
        f"узнавали о своих дырах раньше, чем о них узнают злоумышленники.\n\n"
        f"Этот канал — не про настройку файрволов и не про чтение CVE. "
        f"Этот канал — для владельцев бизнеса, e-commerce директоров, "
        f"основателей SaaS и всех, у кого есть сайт, клиенты и данные, "
        f"которые страшно потерять.\n\n"
        f"<b>Что вы здесь будете читать:</b>\n\n"
        f"🟢 <b>Утечки и атаки</b> — что произошло вчера, без воды и "
        f"технических лекций. Только то, что важно вам как владельцу.\n"
        f"🟢 <b>Разборы реальных взломов</b> — как именно вошли, что украли, "
        f"как этого можно было избежать.\n"
        f"🟢 <b>Лайфхаки на 5 минут</b> — что проверить в своём бизнесе сегодня, "
        f"чтобы не стать следующим кейсом.\n"
        f"🟢 <b>Тренды и закон</b> — 152-ФЗ, штрафы, новые виды атак на "
        f"e-commerce и финтех.\n\n"
        f"Тон у нас простой: без галстуков, без «комплексных решений», без "
        f"«хакера в капюшоне». Только факты, цифры и язык, который понимает "
        f"предприниматель.\n\n"
        f"———\n\n"
        f"<b>Что мы делаем за деньги:</b>\n\n"
        f"🟢 <b>Пентест внешнего периметра</b> — атакуем ваш сайт так, как это "
        f"сделал бы реальный злоумышленник\n"
        f"🟢 <b>Red Team</b> — полная симуляция атаки на компанию, включая "
        f"сотрудников и инфраструктуру\n"
        f"🟢 <b>Защита от DDoS и защищённый хостинг</b>\n"
        f"🟢 <b>Сопровождение по 152-ФЗ</b> и закрытие требований ФСТЭК\n\n"
        f"<b>Что мы делаем бесплатно:</b>\n\n"
        f"🟢 <b>Экспресс-аудит сайта за 15 минут.</b> Смотрим снаружи, "
        f"показываем 1-2 конкретные зоны риска. Без презентаций, без "
        f"продажных звонков.\n\n"
        f"———\n\n"
        f"Если у вас есть сайт с клиентами — напишите в {CONTACT_HANDLE}. "
        f"Посмотрим. Если найдём что-то серьёзное — будете знать раньше, "
        f"чем кто-либо другой.\n\n"
        f"<i>Работаем в тени, чтобы ваш бизнес оставался на свету.</i>\n\n"
        f"🌐 veles-it.ru\n"
        f"📨 {CONTACT_HANDLE} — для бесплатного аудита\n\n"
        f"{CHANNEL_HANDLE}"
    )
    return {
        "text": text,
        "img_category": "Знакомство",
        "img_headline": "Взламываем раньше, чем это сделают другие",
        "img_dek": "Канал Велес IT — кибербезопасность языком предпринимателя",
        "img_filename": "welcome.png",
        "img_fact_label": "минут на бесплатный аудит вашего сайта",
        "img_fact_value": "15",
        "kind": "welcome",
    }


def build_daily_pack(news_items, is_friday=False):
    if len(news_items) < 2:
        raise ValueError("Минимум 2 новости")
    posts = [
        build_news_post(news_items[0]),
        build_news_post(news_items[1]),
    ]
    if is_friday and len(news_items) >= 3:
        posts.append(build_breakdown_post(news_items[2]))
    else:
        posts.append(build_lifehack_post())
    return posts

# Стратегия роста @cyberveles до 1000+ подписчиков и лидов на пентест

## Цель
Не подписчики ради цифры. **Цель — лиды на бесплатный аудит.**
Метрика № 1: количество обращений в @velesit_audit в неделю.
Метрика № 2: % пользователей канала, которые написали в @velesit_audit.

---

## Этап 0. Подготовка фундамента (1–2 дня)

### Что должно быть до первого закупа рекламы:

- [ ] **Закреплён welcome-пост** (image + text из бота)
- [ ] **Минимум 8–10 постов в канале** — иначе человек заходит в пустоту и отписывается
  - Совет: запусти бота 3–4 раза вручную с интервалом, чтобы за 2 дня набить контент
- [ ] **Аватар канала** = логотип V с сайта (бирюза на чёрном)
- [ ] **Описание канала** до 255 символов:
  ```
  Кибербезопасность для бизнеса языком предпринимателя.
  Утечки, разборы атак, лайфхаки. Без воды и хакеров в капюшоне.
  → veles-it.ru | Бесплатный аудит: @velesit_audit
  ```
- [ ] **Comments включены** — для соцдоказательств в постах
- [ ] **Бот @velesit_audit** настроен — отвечает в первую минуту, иначе лиды утекают

---

## Этап 1. Первая 1000 подписчиков (1–2 месяца)

### А. Личный круг + сетка партнёров (300–500 подписчиков, бесплатно)

1. **Сообщение всем владельцам Велес IT** — пусть кинут в свои чаты, друзьям-предпринимателям
2. **Партнёрский обмен** с 3–5 каналами смежных тем:
   - Юристы по 152-ФЗ (общий клиент — владелец интернет-магазина)
   - Хостинги и облака (Selectel, Beget — хотя у них свои каналы, но мелкие хостинги охотно меняются)
   - Каналы для e-commerce-предпринимателей
   - Telegram-каналы про Shopify/Битрикс/Wordpress
3. **Гостевые посты в смежных каналах** — отдай им хороший разбор взлома магазина бесплатно, со ссылкой на @cyberveles

### Б. Закуп в нужных каналах (500–700 подписчиков, ~30–80К ₽)

**Где брать каналы:**
- TGStat → раздел Бизнес → подкатегории "E-commerce", "Маркетинг", "Стартапы"
- Telega.in / Combot — биржи рекламы
- Ручной поиск: ищи каналы 5–30К подписчиков с ER >5%

**Что искать (по приоритету):**

| Категория | Почему | Пример формулировок поиска |
|---|---|---|
| Каналы для e-commerce | Прямая ЦА — у них база клиентов | "ecommerce рунет", "интернет магазин запуск" |
| Каналы для предпринимателей | Широкая ЦА с правильными болями | "бизнес и стартап", "предприниматель" |
| Каналы про SaaS / финтех | У них больше всего страха перед утечками | "SaaS", "финтех", "стартап Россия" |
| Каналы про маркетплейсы | Огромная ЦА с тонкой защитой | "wildberries продавцам", "ozon селлер" |

**Критерии хорошего канала:**
- ER (engagement rate) **больше 5%** — иначе мёртвая аудитория
- В топе постов есть посты с обсуждением и реакциями
- Аватарки подписчиков выглядят живыми (не аноним-боты)
- Лента не из репостов, есть свой контент
- В рекламе нет крипты, казино, инвест-схем (значит, владелец отбирает партнёров)

**Креативы для закупа** (всегда 2–3 варианта на A/B):

**Вариант 1 — через факт (новостной канал):**
> 🛡 Хороший канал для тех, у кого есть сайт с клиентами.
> 
> На прошлой неделе слили 14 миллионов записей у крупного ритейлера. На позапрошлой — лёг магазин в чёрную пятницу на 7 часов. Эти истории — не про крупняк. Они уже добрались до среднего бизнеса.
> 
> @cyberveles — канал о том, как сегодня взламывают такие компании, как ваша. Без воды и хакеров в капюшоне. Утечки, разборы, чек-листы на 5 минут.
> 
> 👉 @cyberveles

**Вариант 2 — через лайфхак (полезная подача):**
> Откройте haveibeenpwned.com и вбейте корпоративную почту вашей компании. Скорее всего, она уже в базах даркнета.
> 
> Это значит, что чьи-то пароли от админки вашего сайта — у злоумышленников. Они их пробуют автоматически на тысячах сайтов в день.
> 
> Подобные простые проверки и разборы реальных взломов — каждый день в @cyberveles.

**Вариант 3 — через социалку (от лица предпринимателя):**
> Подписался на @cyberveles. Это первый канал по кибербезу, который читается без зевков и не пытается продать на каждом шагу. Реально полезно тем, у кого есть онлайн-бизнес.

### В. Бартер с инфлюенсерами (бесплатно или 5–10К ₽)

Свяжись с авторами небольших каналов про предпринимательство и e-commerce (1–10К подписчиков). Предложи им **бесплатный пентест** в обмен на упоминание. Это win-win:
- Им — реальная ценность (отчёт о состоянии их сайта)
- Вам — нативный пост от лица человека, которого их аудитория уважает

**Скрипт сообщения:**
> Привет, [имя]. Читаю ваш канал. Мы — Велес IT, делаем пентесты для бизнеса. Готовы провести вам полный аудит сайта бесплатно (обычно стоит 60–80К) — посмотрим где у вас дыры, дадим отчёт. Если результаты будут полезные — буду благодарен за репост или упоминание @cyberveles. Если нет — без обязательств.

---

## Этап 2. От 1000 к 5000 (3–4 месяца)

### А. Контентные хиты (вирусный охват)

**Раз в 2 недели делай "карточку дня" с цифрой**, которую захотят репостить:

> 80% атак на бизнес начинаются с одного письма
> 
> Источник: PT Security Report 2025
> 
> Что это значит: ваш главный безопасник — не файрвол, а сотрудник, который проверяет письма в обеденный перерыв.

Такие посты делятся в личке, в чатах, в каналах коллег.

### Б. Кейс-разборы (привлечение через VC.ru, Habr, Telegra.ph)

Раз в месяц — **большой публичный разбор реального взлома** под названием типа:
> "Как магазин на Shopify потерял 47 000 карт за 3 недели — и что вы должны проверить уже сегодня"

Публикуешь:
1. На VC.ru (раздел "Кейс")
2. На Habr (Hub Information Security)
3. На Telegra.ph
4. В @cyberveles короткую версию + ссылка на полный лонгрид

Хороший лонгрид на VC.ru приносит 100–500 подписчиков сам собой.

### В. Лид-магнит — чек-лист на 5 минут

Сделай **PDF-чек-лист "Безопасность интернет-магазина за 5 минут"** (можно через бот — даём за подписку).

Структура:
- 10 пунктов проверки
- Бренд Велес IT
- Финал: "Прошли все 10? Молодцы. Не прошли хотя бы 3? Вот ссылка на бесплатный аудит → @velesit_audit"

Лид-магнит = повод сделать рекламу с CTA "Получи чек-лист бесплатно", это конвертит в 2–3 раза лучше, чем "Подпишись".

---

## Этап 3. Лидогенерация (постоянно)

### Воронка (как лиды превращаются в клиентов)

```
1. Закуп в Telegram-канале / органика  
   ↓ (200–500 переходов)
2. Открывает @cyberveles — видит закреп welcome  
   ↓ (50–100 подписываются)
3. Читает 3–5 постов в течение недели  
   ↓ (5–15 решают написать)
4. Пишет в @velesit_audit  
   ↓ (ответ за 5 минут от живого человека)
5. Бесплатный 15-минутный аудит  
   ↓ (нашли 1–2 дыры → отчёт)
6. Из них 20–30% становятся клиентами на платный пентест
```

### Что делает @velesit_audit (важно!)

Бот не отвечает шаблонами. На @velesit_audit должен сидеть **человек**, который:
1. Отвечает в первую минуту
2. Спрашивает: "Какой у вас сайт? Дайте URL — посмотрю минут за 15"
3. Реально проверяет 1–2 точки (можно nuclei-сканер запустить)
4. Возвращается с конкретикой: "Нашёл вот это и это, держите отчёт"
5. Не давит. Если интересно платно — предлагает звонок. Не давит.

Это и есть стратегия Велес IT с сайта: **«Без галстуков. Только мясо.»**

---

## Бюджет на старт

| Статья | Стоимость | Эффект |
|---|---|---|
| Прогрев канала контентом (бот) | 0 ₽ | +20–30 постов |
| Партнёрский обмен | 0 ₽ | 200–400 подписок |
| Закуп в 5–7 каналах | 30–80К ₽ | 500–700 подписок |
| Бартер пентестом | 0–10К ₽ | Нативные упоминания |
| Лид-магнит (PDF) | 5К ₽ | +30% к конверсии в подписку |
| Лонгриды на VC/Habr | 0 ₽ | 100–500 подписок за лонгрид |
| **Итого первый месяц** | **35–95К ₽** | **1000–1500 подписчиков** |

Стоимость подписчика: 30–80 ₽
Стоимость лида (написал в @velesit_audit): 800–2000 ₽
Стоимость клиента на пентест (LTV 60–300К): 5–15К ₽

**Окупаемость с первого клиента — да.** С 5-го месяца канал работает в плюс.

---

## Что НЕ делать

❌ **Не покупать подписчиков** — мёртвые подписки убивают ER, и Telegram режет охваты живой аудитории
❌ **Не лить трафик из ВК/Я.Директа** — там нет ЦА, которая платит за пентест
❌ **Не делать гивэвеи "iPhone за подписку"** — приведут шопоголиков, не предпринимателей
❌ **Не постить чаще 2 раз в день** — выжигаешь аудиторию, отпишутся
❌ **Не делать "разборы хакерских группировок"** — это для безопасников, не для ЦА

"""
image_generator.py — генерация постовых картинок в фирстиле Велес IT.
Палитра снята со скриншота сайта veles-it.ru:
  - фон #0A1414
  - карточка #0F1A1A
  - бирюза #5EE6D0 (главный акцент)
  - оранжевый #FF8C42 (alert / breakdown)
  - текст #E6F4F1 / #7A9B96
"""
import os
from datetime import datetime
from PIL import Image, ImageDraw, ImageFont
from config import IMAGES_DIR

WIDTH, HEIGHT = 1080, 1080

# === Брендовая палитра Велес IT ===
BG = (10, 20, 20)              # #0A1414
CARD = (15, 26, 26)            # #0F1A1A
CARD_BORDER = (30, 60, 58)
TURQUOISE = (94, 230, 208)     # #5EE6D0
TURQUOISE_DIM = (60, 145, 130)
ORANGE = (255, 140, 66)        # #FF8C42
TEXT_MAIN = (230, 244, 241)    # #E6F4F1
TEXT_DIM = (122, 155, 150)     # #7A9B96
TEXT_FAINT = (60, 85, 82)
GRID = (16, 28, 28)

# Контекстные индикаторы по типу поста
INDICATORS = {
    "news":      ("threat.detected",   TURQUOISE),
    "breakdown": ("incident.analyzed", ORANGE),
    "lifehack":  ("action.required",   TURQUOISE),
    "welcome":   ("SOC · online",      TURQUOISE),
    "stat":      ("data.tracked",      TURQUOISE),
    "law":       ("policy.update",     TURQUOISE),
}


def generate_image_prompt(topic: str) -> str:
    return (
        f"Premium B2B cybersecurity poster, very dark teal background (#0A1414), "
        f"turquoise accent (#5EE6D0), faint geometric grid, monitoring dashboard aesthetic. "
        f"Editorial typography, spacious composition. NO hooded hacker, NO matrix code. "
        f"Theme: {topic}. Square 1080x1080."
    )


def _font(size, bold=False):
    candidates = [
        "/System/Library/Fonts/Supplemental/Arial Bold.ttf" if bold else "/System/Library/Fonts/Supplemental/Arial.ttf",
        "/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf" if bold else "/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf",
        "C:/Windows/Fonts/arialbd.ttf" if bold else "C:/Windows/Fonts/arial.ttf",
    ]
    for path in candidates:
        if os.path.exists(path):
            try:
                return ImageFont.truetype(path, size)
            except Exception:
                continue
    return ImageFont.load_default()


def _wrap(draw, text, font, max_width):
    words = text.split()
    lines, cur = [], ""
    for w in words:
        trial = (cur + " " + w).strip()
        if draw.textlength(trial, font=font) <= max_width:
            cur = trial
        else:
            if cur:
                lines.append(cur)
            cur = w
    if cur:
        lines.append(cur)
    return lines


def _draw_v_logo(draw, x, y, size=44):
    """Логотип V — стилизованный щит, как на сайте."""
    points_outer = [
        (x, y),
        (x + size, y),
        (x + size, y + size * 0.55),
        (x + size / 2, y + size),
        (x, y + size * 0.55),
    ]
    draw.polygon(points_outer, outline=TURQUOISE, fill=None, width=2)
    pad = size * 0.22
    inner = [
        (x + pad, y + pad),
        (x + size - pad, y + pad),
        (x + size / 2, y + size - pad * 1.2),
    ]
    draw.polygon(inner, fill=TURQUOISE)


def _draw_grid(draw):
    step = 60
    for x in range(0, WIDTH, step):
        draw.line([(x, 0), (x, HEIGHT)], fill=GRID, width=1)
    for y in range(0, HEIGHT, step):
        draw.line([(0, y), (WIDTH, y)], fill=GRID, width=1)


def create_post_image(
    category: str,
    headline: str,
    dek: str,
    kind: str = "news",
    fact_label: str = None,
    fact_value: str = None,
    filename: str = "post.png",
    channel_handle: str = "@cyberveles",
    site_url: str = "veles-it.ru",
) -> str:
    """Создаёт картинку поста в фирстиле Велес IT. Возвращает путь."""
    os.makedirs(IMAGES_DIR, exist_ok=True)
    path = os.path.join(IMAGES_DIR, filename)

    img = Image.new("RGB", (WIDTH, HEIGHT), BG)
    draw = ImageDraw.Draw(img)

    _draw_grid(draw)

    # ШАПКА
    _draw_v_logo(draw, 70, 70, size=44)
    draw.text((128, 76), "Велес IT", font=_font(28, bold=True), fill=TEXT_MAIN)

    date_str = datetime.now().strftime("%d.%m.%Y")
    meta_text = f"{kind.upper()}  ·  {date_str}"
    meta_w = draw.textlength(meta_text, font=_font(20))
    draw.text((WIDTH - 70 - meta_w, 84), meta_text, font=_font(20), fill=TEXT_DIM)

    draw.line([(70, 145), (WIDTH - 70, 145)], fill=TURQUOISE_DIM, width=1)

    # РУБРИКА
    cat_y = 200
    draw.line([(70, cat_y + 14), (105, cat_y + 14)], fill=TURQUOISE, width=2)
    draw.text((118, cat_y), category.upper(),
              font=_font(22, bold=True), fill=TURQUOISE)

    # ЗАГОЛОВОК (адаптивный размер под длину)
    char_count = len(headline)
    if char_count <= 30:   title_size = 92
    elif char_count <= 50: title_size = 80
    elif char_count <= 75: title_size = 68
    else:                  title_size = 58

    title_font = _font(title_size, bold=True)
    title_lines = _wrap(draw, headline, title_font, WIDTH - 140)[:5]
    line_height = int(title_size * 1.15)
    y = 260
    for line in title_lines:
        draw.text((70, y), line, font=title_font, fill=TEXT_MAIN)
        y += line_height

    # ДЕК
    y += 20
    dek_font = _font(30)
    for line in _wrap(draw, dek, dek_font, WIDTH - 200)[:3]:
        draw.text((70, y), line, font=dek_font, fill=TEXT_DIM)
        y += 42

    # ФАКТОИД
    if fact_label and fact_value:
        box_y = HEIGHT - 320
        box_h = 150
        draw.rounded_rectangle(
            [70, box_y, WIDTH - 70, box_y + box_h],
            radius=18, fill=CARD, outline=CARD_BORDER, width=1
        )
        value_size = 64 if len(fact_value) <= 8 else 52 if len(fact_value) <= 12 else 44
        draw.text((100, box_y + 18), fact_value,
                  font=_font(value_size, bold=True), fill=TURQUOISE)
        draw.text((100, box_y + 95), fact_label.lower(),
                  font=_font(22), fill=TEXT_DIM)

        indicator_text, indicator_color = INDICATORS.get(kind, INDICATORS["news"])
        ind_w = draw.textlength(indicator_text, font=_font(20))
        ind_x = WIDTH - 100 - ind_w - 22
        ind_y = box_y + 30
        draw.ellipse([ind_x, ind_y, ind_x + 12, ind_y + 12], fill=indicator_color)
        draw.text((ind_x + 22, ind_y - 4), indicator_text,
                  font=_font(20), fill=TEXT_DIM)

    # ПОДВАЛ
    footer_y = HEIGHT - 90
    draw.line([(70, footer_y), (WIDTH - 70, footer_y)], fill=TEXT_FAINT, width=1)
    draw.text((70, footer_y + 25), channel_handle,
              font=_font(22), fill=TEXT_DIM)
    cta_text = f"{site_url}  →"
    cta_w = draw.textlength(cta_text, font=_font(22, bold=True))
    draw.text((WIDTH - 70 - cta_w, footer_y + 25), cta_text,
              font=_font(22, bold=True), fill=TURQUOISE)

    img.save(path, "PNG", optimize=True)
    return path


# Совместимость со старым API generator.py
def create_placeholder_image(title, subtitle, filename, kind="news",
                              fact_label="минут на бесплатный аудит",
                              fact_value="15"):
    category_map = {
        "news": "Новость", "breakdown": "Разбор", "lifehack": "Лайфхак",
        "welcome": "Знакомство", "stat": "Цифра дня", "law": "Закон",
    }
    return create_post_image(
        category=category_map.get(kind, "Новость"),
        headline=title, dek=subtitle, kind=kind,
        fact_label=fact_label, fact_value=fact_value,
        filename=filename,
    )

# Велес IT — Telegram-бот @cyberveles

Автоматический контент-завод для Telegram-канала Велес IT.
Парсит новости кибербезопасности → переписывает в фирменный голос →
генерирует картинки в брендстиле → постит в @cyberveles.

## 🎯 Что делает бот

За один запуск (раз в день):
- Парсит RSS из 6 источников ИБ (The Hacker News, Bleeping Computer, Krebs и др.)
- Фильтрует по релевантным темам (утечки, ransomware, e-commerce, DDoS, API)
- Берёт 3 темы из разных категорий
- Переписывает каждую в голос Велес IT (через шаблоны или Claude API)
- Генерирует картинку 1080×1080 в фирменном стиле
- Шлёт в канал (или на одобрение в личку — режим draft)

В пятницу — пост-разбор вместо лайфхака.

## 📁 Структура

```
veles_bot/
├── main.py              # Оркестратор (run | welcome | moderate)
├── sources.py           # RSS-парсинг
├── filter.py            # Фильтр по ключевым словам
├── ai_writer.py         # Перефраз в голос Велес IT (шаблоны + Claude API)
├── generator.py         # Сборка постов из блоков
├── image_generator.py   # Картинки в брендстиле
├── telegram_sender.py   # Bot API + draft-модерация
├── config.py            # Все настройки
├── requirements.txt
├── .env.example
├── .github/workflows/
│   ├── daily.yml        # Автозапуск раз в день (cron)
│   └── moderate.yml     # Поллинг кнопок ✅/❌
└── GROWTH_STRATEGY.md   # Стратегия роста канала
```

---

## 🚀 ПОШАГОВЫЙ ЗАПУСК

### Шаг 1. Создать бота

1. В Telegram открой [@BotFather](https://t.me/BotFather)
2. `/newbot` → имя `Велес IT News` → username `veles_it_news_bot` (любой свободный)
3. Получи **токен** вида `123456:ABC...` → сохрани

### Шаг 2. Подготовить канал

1. Создай канал @cyberveles (если ещё нет)
2. Добавь бота из шага 1 как **админа** с правом «Публиковать сообщения»
3. Узнай свой Telegram ID:
   - Открой [@userinfobot](https://t.me/userinfobot) → `/start`
   - Получи число вида `123456789` — это твой `TELEGRAM_ADMIN_ID`

### Шаг 3. Запуск (выбери способ)

---

### Способ А — GitHub Actions (бесплатно, работает 24/7)

Это лучший вариант для production.

1. **Создай репозиторий на GitHub** (private — приватный)
2. Залей все файлы из этого проекта
3. В репозитории → **Settings → Secrets and variables → Actions** → New repository secret. Добавь:

| Имя секрета | Значение |
|---|---|
| `TELEGRAM_BOT_TOKEN` | Токен от BotFather |
| `TELEGRAM_CHANNEL_ID` | `@cyberveles` |
| `TELEGRAM_ADMIN_ID` | Твой числовой ID |
| `MODERATION_MODE` | `auto` (или `draft` — см. ниже) |
| `SITE_URL` | `https://veles-it.ru` |
| `CONTACT_HANDLE` | `@velesit_audit` |
| `ANTHROPIC_API_KEY` | (опционально — для живого AI-текста) |

4. Перейди в **Actions** → выбери **Veles Daily Post** → **Run workflow** → выбери `welcome` → Run.
   Это опубликует приветственный пост.
5. **Закрепи welcome-пост** в канале вручную.
6. Дальше бот будет постить каждый день в 10:00 МСК автоматически (cron в `.github/workflows/daily.yml`).
7. Менять время — правишь строку `cron: '0 7 * * *'` (UTC).

---

### Способ Б — Replit (с iPhone, для draft-режима)

Это вариант если хочется одобрять каждый пост вручную через ✅/❌ в личке.

1. Поставь приложение **Replit** из App Store
2. **Create Repl** → шаблон **Python** → имя `veles-bot`
3. Скопируй все файлы проекта (можно загрузить как zip)
4. **🔒 Secrets** (слева) → добавь все переменные из таблицы выше
5. В консоли запусти `pip install -r requirements.txt`
6. Запусти приветственный пост: в Shell → `python main.py welcome`
7. Запусти основной цикл: `python main.py`
8. Параллельно держи открытой вкладку с командой `python main.py moderate` (это слушает кнопки в личке)
9. Для **Always On** (чтобы работало без iPhone) — нужен платный план Replit ($7/мес)

---

### Способ В — Свой ноут / сервер

```bash
git clone <твой-репо> veles_bot
cd veles_bot
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# открой .env и заполни значения

# Первый запуск — приветственный пост
python main.py welcome

# Закрепи welcome в канале (вручную)

# Дальше тестовый запуск
python main.py

# Если режим draft — параллельно в другом терминале:
python main.py moderate

# Cron на сервере (каждый день в 10:00):
# 0 10 * * * cd /path/to/veles_bot && /path/to/venv/bin/python main.py >> bot.log 2>&1
```

---

## 🤖 Режимы работы

### `MODERATION_MODE=auto`
Бот публикует посты сразу в канал.
**Подходит для:** GitHub Actions, когда уверен в качестве шаблонов.

### `MODERATION_MODE=draft`  *(рекомендуется на старте)*
Бот шлёт посты в личку админу с кнопками ✅/❌.
- ✅ Опубликовать → пост улетает в канал
- ❌ Отклонить → пропускается

**Подходит для:** первых недель работы, чтобы убедиться, что AI / шаблоны попадают в тон.

⚠️ Для draft-режима нужен постоянно работающий процесс модерации (`python main.py moderate`).
GitHub Actions с поллингом раз в 5 минут — компромисс, кнопки будут срабатывать с задержкой.

---

## 🧠 Включить живой AI-текст (опционально)

По умолчанию бот использует шаблоны — стабильно, бесплатно, чуть однообразно.

Чтобы тексты стали живыми и разнообразными:

1. Получи API-ключ: [console.anthropic.com](https://console.anthropic.com) → API Keys
2. Положи $5 на счёт (хватит на ~1500 постов с моделью Haiku 4.5)
3. Добавь в `.env` или GitHub Secrets:
   ```
   ANTHROPIC_API_KEY=sk-ant-...
   ```
4. Готово — бот сам подхватит ключ и начнёт переписывать тексты Claude'ом.

Стоимость: при модели `claude-haiku-4-5` ~$0.003 за пост (~30 копеек).

Хочешь премиум-качество — поменяй в `ai_writer.py` строку:
```python
model="claude-haiku-4-5-20251001"
```
на:
```python
model="claude-opus-4-7"
```
Будет дороже (~$0.05 за пост = 5 рублей), но тексты ощутимо лучше.

---

## 📝 Что делать на старте канала

### Чек-лист первого дня:

- [ ] Опубликовать welcome-пост: `python main.py welcome`
- [ ] **Закрепить** welcome-пост в канале вручную (Telegram → пост → 📌)
- [ ] Запустить бота 3–4 раза подряд с интервалом 1–2 часа,
      чтобы за день набить 8–10 постов
- [ ] Поставить аватар канала (логотип V с сайта)
- [ ] Заполнить описание канала (текст в `GROWTH_STRATEGY.md`)
- [ ] Включить комментарии в канале

### Дальше:

- Автозапуск раз в день
- Раз в неделю проверяй ленту: попадает ли тон в фирменный голос?
- Раз в месяц — большой контент-кейс на VC.ru со ссылкой на канал

Подробнее — `GROWTH_STRATEGY.md`.

---

## 🛠 Команды

```bash
python main.py              # Постит дневной пакет (3 поста)
python main.py welcome      # Публикует приветственный пост
python main.py moderate     # Слушает кнопки ✅/❌ (для draft-режима)
```

---

## 📊 Метрики

Что отслеживать:
- **Прирост подписчиков** (TGStat / Telemetr)
- **ER (engagement rate)** — нормально 5–15%
- **Переходы в @velesit_audit** — главная метрика, цель 5–15 в неделю
- **% подписчиков, написавших в @velesit_audit** — норма 0.5–1%

Не гонись за цифрой подписчиков. **Канал работает, если приносит лиды.**

---

## ❓ FAQ

**Q: Бот молчит в канале — что не так?**
A: Проверь логи (GitHub Actions → последний run → раскрой "Run bot"). Чаще всего:
1. Неправильный токен (TELEGRAM_BOT_TOKEN)
2. Бот не админ канала
3. Канал не заполнен в TELEGRAM_CHANNEL_ID (должно быть `@cyberveles`)

**Q: Тексты однообразные.**
A: Включи Claude API (см. выше).

**Q: Постит слишком технические темы.**
A: Поправь `KEYWORDS_INCLUDE` в `config.py` — оставь только то, что важно ЦА.

**Q: Картинки кривые / шрифт не отображается.**
A: На macOS/Linux используется DejaVuSans или Arial. Проверь, что эти шрифты установлены. На сервере:
```bash
sudo apt-get install -y fonts-dejavu-core
```

**Q: Хочу постить два раза в день.**
A: В `.github/workflows/daily.yml` поменяй `cron: '0 7 * * *'` на:
```yaml
- cron: '0 7 * * *'    # 10:00 МСК
- cron: '0 14 * * *'   # 17:00 МСК
```

---

## 🎨 Дизайн-система

Палитра (снято с veles-it.ru):
- Фон: `#0A1414`
- Карточка: `#0F1A1A`
- Бирюза: `#5EE6D0` (главный акцент)
- Оранжевый: `#FF8C42` (alert / breakdown)
- Текст: `#E6F4F1` / `#7A9B96`

Картинки 1080×1080. Все типы постов имеют свой ярлык-индикатор:
- `news` → `threat.detected` (бирюза)
- `breakdown` → `incident.analyzed` (оранжевый)
- `lifehack` → `action.required`
- `welcome` → `SOC · online`
- `stat` → `data.tracked`

---

## 📞 Контакты

- Сайт: https://veles-it.ru
- Канал: @cyberveles
- Аудит: @velesit_audit

requests==2.31.0
feedparser==6.0.11
beautifulsoup4==4.12.3
python-dotenv==1.0.1
schedule==1.2.1
Pillow==10.2.0
anthropic==0.39.0

import json
import os
import feedparser
from bs4 import BeautifulSoup
from config import RSS_FEEDS, SENT_FILE


def load_sent():
    if not os.path.exists(SENT_FILE):
        return []
    try:
        with open(SENT_FILE, "r", encoding="utf-8") as f:
            return json.load(f)
    except Exception:
        return []


def save_sent(sent_list):
    sent_list = sent_list[-500:]
    with open(SENT_FILE, "w", encoding="utf-8") as f:
        json.dump(sent_list, f, ensure_ascii=False, indent=2)


def _clean(html_text: str) -> str:
    if not html_text:
        return ""
    soup = BeautifulSoup(html_text, "html.parser")
    return soup.get_text(" ", strip=True)[:800]


def fetch_news(limit_per_feed=20):
    sent = set(load_sent())
    items = []

    for feed_url in RSS_FEEDS:
        try:
            parsed = feedparser.parse(feed_url)
            for entry in parsed.entries[:limit_per_feed]:
                link = entry.get("link", "").strip()
                if not link or link in sent:
                    continue
                items.append({
                    "title": entry.get("title", "").strip(),
                    "summary": _clean(entry.get("summary") or entry.get("description", "")),
                    "link": link,
                    "source": parsed.feed.get("title", "Source"),
                })
        except Exception as e:
            print(f"[sources] err {feed_url}: {e}")

    return items

import json
import os
import time
import requests
from config import (
    TELEGRAM_BOT_TOKEN, TELEGRAM_CHANNEL_ID, TELEGRAM_ADMIN_ID,
    MODERATION_MODE, PENDING_FILE
)

API = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}"


def _send_text(chat_id, text, reply_markup=None):
    data = {
        "chat_id": chat_id,
        "text": text,
        "parse_mode": "HTML",
        "disable_web_page_preview": True,
    }
    if reply_markup:
        data["reply_markup"] = json.dumps(reply_markup)
    r = requests.post(f"{API}/sendMessage", data=data, timeout=30)
    return r.json() if r.ok else None


def _send_photo(chat_id, image_path, caption, reply_markup=None):
    if len(caption) > 1024:
        with open(image_path, "rb") as f:
            requests.post(
                f"{API}/sendPhoto",
                data={"chat_id": chat_id},
                files={"photo": f},
                timeout=60,
            )
        return _send_text(chat_id, caption, reply_markup)

    with open(image_path, "rb") as f:
        data = {"chat_id": chat_id, "caption": caption, "parse_mode": "HTML"}
        if reply_markup:
            data["reply_markup"] = json.dumps(reply_markup)
        r = requests.post(f"{API}/sendPhoto", data=data, files={"photo": f}, timeout=60)
    if r.ok:
        return r.json()
    print(f"[telegram] sendPhoto failed: {r.text}")
    return _send_text(chat_id, caption, reply_markup)


def _load_pending():
    if not os.path.exists(PENDING_FILE):
        return {}
    with open(PENDING_FILE, "r", encoding="utf-8") as f:
        return json.load(f)


def _save_pending(data):
    with open(PENDING_FILE, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)


def publish(post: dict, image_path: str):
    """Публикует или шлёт админу на одобрение в зависимости от режима."""
    if MODERATION_MODE == "auto":
        return _send_photo(TELEGRAM_CHANNEL_ID, image_path, post["text"])

    pending = _load_pending()
    post_id = str(int(time.time() * 1000))
    pending[post_id] = {
        "text": post["text"],
        "image_path": image_path,
        "kind": post["kind"],
    }
    _save_pending(pending)

    keyboard = {
        "inline_keyboard": [[
            {"text": "✅ Опубликовать", "callback_data": f"publish:{post_id}"},
            {"text": "❌ Отклонить", "callback_data": f"reject:{post_id}"},
        ]]
    }
    return _send_photo(TELEGRAM_ADMIN_ID, image_path, post["text"], keyboard)


def poll_and_handle_callbacks():
    """Поллинг кнопок ✅/❌ от админа."""
    offset_file = ".tg_offset"
    offset = 0
    if os.path.exists(offset_file):
        with open(offset_file) as f:
            offset = int(f.read().strip() or 0)

    r = requests.get(f"{API}/getUpdates", params={"offset": offset, "timeout": 5})
    if not r.ok:
        return

    updates = r.json().get("result", [])
    for upd in updates:
        offset = upd["update_id"] + 1
        cq = upd.get("callback_query")
        if not cq:
            continue

        data = cq.get("data", "")
        if ":" not in data:
            continue
        action, post_id = data.split(":", 1)

        pending = _load_pending()
        post = pending.get(post_id)
        if not post:
            requests.post(f"{API}/answerCallbackQuery",
                          data={"callback_query_id": cq["id"], "text": "Уже обработано"})
            continue

        if action == "publish":
            _send_photo(TELEGRAM_CHANNEL_ID, post["image_path"], post["text"])
            requests.post(f"{API}/answerCallbackQuery",
                          data={"callback_query_id": cq["id"], "text": "✅ Опубликовано"})
        else:
            requests.post(f"{API}/answerCallbackQuery",
                          data={"callback_query_id": cq["id"], "text": "❌ Отклонено"})

        del pending[post_id]
        _save_pending(pending)

    with open(offset_file, "w") as f:
        f.write(str(offset))
